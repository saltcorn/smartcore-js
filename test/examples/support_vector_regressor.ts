import assert from 'assert'

export default () => {
  it('Support Vector Regressor (SVR)', () => {
    assert.fail(
      "Failed to reproduce this example due to type mismatches. In particular, the return type of 'svrF32U32.predict(x_test)'.",
    )
  })
}
